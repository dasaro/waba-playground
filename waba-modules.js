// AUTO-GENERATED by scripts/sync-modules.js
// DO NOT EDIT MANUALLY
// Last updated: 2026-03-15T11:56:27.265Z

export const wabaModules = {
    core: {
        "base": `%% WABA Base Logic (Semiring/Monoid-Independent) - GROUNDING OPTIMIZED
%% This file contains the core argumentation logic that is independent
%% of the choice of semiring (weight propagation) and monoid (cost aggregation).
%%
%% OPTIMIZATIONS APPLIED:
%% - Added unary domain predicates to reduce grounding size
%% - Use rule/1 consistently instead of head(R,_)
%% - Removed redundant supported(X) joins

%% Budget constant used by the supported CLI wrapper and by raw clingo runs.
%% Framework files may also state budget(beta); duplicate identical facts are harmless.

%% ====================
%% DOMAIN PREDICATES
%% ====================
%% These unary predicates reduce grounding by avoiding repeated existential patterns

%% Rule meta-predicate (R is a rule identifier)
rule(R) :- head(R,_).

%% Unary flag: X appears as some rule head
is_head(X) :- head(_,X).

%% Unary flag: R has at least one body element
has_body(R) :- body(R,_).

%% Derived atoms: heads that are not assumptions
derived_atom(X) :- is_head(X), not assumption(X).

%% ====================
%% CORE LOGIC
%% ====================

%% Support computation (weight-independent)
%% An atom is supported if:
%% 1. It's an assumption that is in, OR
%% 2. There exists a rule with this atom as head and all body elements are supported
supported(X) :- assumption(X), in(X).
supported(X) :- head(R,X), triggered_by_in(R).

%% A rule is triggered when all its body elements are supported
%% OPTIMIZED: Use rule(R) instead of head(R,_) for domain
triggered_by_in(R) :- rule(R), supported(X) : body(R,X).

%% Attack computation
%% Attacks are keyed by provenance seed-set and target assumption.
%% The semiring layer therefore provides provenance-aware support globally,
%% independently of the current candidate extension.
attack(attack(S,Y),S,X,Y,W) :-
    supported_with_weight_from(X,S,W),
    assumption(Y),
    contrary(Y,X).

attacks_with_weight(X,Y,W) :- attack(_,_,X,Y,W).

source_id(S) :- supported_with_weight_from(_,S,_).

source_subset(T,S) :-
    source_id(T),
    source_id(S),
    T & S = T.

seed_member(S,A) :-
    source_id(S),
    assumption(A),
    source_bit(A,B),
    S != 0,
    S & B = B.

source_member(S,A) :- seed_member(S,A).

closure_member(S,A) :-
    source_id(S),
    assumption(A),
    supported_with_weight_from(A,T,_),
    source_subset(T,S).

source_disabled_by_out(S) :-
    seed_member(S,A),
    not in(A).

source_enabled_by_in(S) :-
    attack(_,S,_,_,_),
    not source_disabled_by_out(S).

:- supported(X), assumption(Y), contrary(Y,X), not supported_with_weight_from(X,_,_).

%% Attack discretion choice
%% We can choose to discard any attack object (at a cost)
{ discarded_attack(Aid) : attack(Aid,_,_,_,_) }.

discarded_attack_detail(Aid,S,X,Y,W) :-
    attack(Aid,S,X,Y,W),
    discarded_attack(Aid).

discarded_attack(X,Y,W) :- discarded_attack_detail(_,_,X,Y,W).

%% Successful attacks
%% An attack succeeds if at least one attack object with that coarse projection survives
attacks_successfully_with_weight(X,Y,W) :-
    attack(Aid,_,X,Y,W),
    not discarded_attack(Aid).

%% ====================
%% BUDGET CONSTRAINT
%% ====================
%% Budget Constraints
%% ====================
%%
%% Budget value is typically set via command line: -c beta=N
budget(beta).

%% Budget constraints are in constraint/ directory.
%% \`constraint/ub.lp\` and \`constraint/lb.lp\` read the aggregate family from
%% the loaded monoid module.
%%
%% Load appropriate constraint file for budget enforcement:
%% - constraint/ub.lp - Upper bound for the active monoid family (cost ≤ beta)
%% - constraint/lb.lp - Lower bound for the active monoid family (cost ≥ beta)
%% - constraint/no_discard.lp - Plain ABA mode (no attack discarding)
`
    },
    semiring: {
        "godel": `%% Gödel Semiring for Weight Propagation (Fuzzy Logic) - GROUNDING OPTIMIZED
%% Semiring: (ℤ ∪ {±∞}, max, min, #inf, #sup)
%% - Domain: All integers plus infinities
%% - Disjunction/⊕ (OR, multiple derivations): max (strongest alternative)
%% - Conjunction/⊗ (AND, body elements): min (weakest link)
%% - Additive identity: #inf (identity for max operation)
%% - Multiplicative identity: #sup (identity for min operation)
%% - Interpretation: weights are truth degrees, higher is better
%%
%% OPTIMIZATIONS APPLIED:
%% - Use not is_head(X) instead of not head(_,X)
%% - Use not has_body(R) instead of not body(R,_)
%% - Use rule(R) consistently for rule domains
%% - Remove redundant supported(X) checks
%%
%% In Gödel fuzzy logic:
%% - Domain: ℤ ∪ {±∞} (integers plus positive/negative infinity)
%% - Conjunction takes the minimum (weakest link in a chain)
%% - Disjunction takes the maximum (strongest of multiple proofs)
%% - Unweighted atoms get #sup (maximum truth, hardest to discard)

active_semiring(godel).
%% Default weight policy selection for unweighted assumptions.
%%
%% Semiring modules provide semiring_default_weight(policy, value).
%% Callers may optionally load one explicit policy module from defaults/.
%% If none is loaded, legacy behavior is preserved.

configured_default_policy :- explicit_default_policy(_).

:- explicit_default_policy(P1), explicit_default_policy(P2), P1 != P2.

active_default_policy(P) :- explicit_default_policy(P).
active_default_policy(legacy) :- not configured_default_policy.

default_assumption_weight(W) :-
    active_default_policy(P),
    semiring_default_weight(P, W).

:- active_default_policy(P), not semiring_default_weight(P, _).


semiring_default_weight(legacy,#sup).
semiring_default_weight(aba,#sup).
semiring_default_weight(neutral,#sup).

%% =============================================
%% CLASSICAL ABA RECOVERY (No Discarding)
%% =============================================
%%
%% To recover plain unweighted ABA behavior with this semiring:
%%
%% UPPER BOUND REGIME (MAX, SUM, COUNT, LEX monoids):
%%   δ = #sup     (default weight for unweighted assumptions)
%%   β = 0        (budget value)
%%   Mechanism: constraint/ub.lp explicitly rejects discarding #sup-weighted attacks
%%              (line 41: \`:- discarded_attack(_,_,#sup), budget(B), B != #sup.\`)
%%
%% LOWER BOUND REGIME (MIN monoid):
%%   Option 1 (recommended):
%%     δ = #inf   (default weight)
%%     β = 0      (budget value)
%%     Mechanism: MIN(#inf) = #inf < 0, rejected by constraint/lb.lp
%%   Option 2:
%%     δ = #sup   (default weight)
%%     β = any    (any finite budget)
%%     Mechanism: constraint/lb.lp explicitly rejects discarding #sup attacks
%%                (line 43: \`:- discarded_attack(_,_,#sup).\`)
%%
%% CURRENT IMPLEMENTATION: δ = #sup (see line ~45 below)
%% This enables ABA recovery for all monoids with appropriate budget settings.

%% ========================================
%% DEFAULT WEIGHT PRINCIPLE: SUPREMUM
%% ========================================
%%
%% Unweighted atoms receive the SUPREMUM (maximum value in range):
%% - Matches standard ABA: assumptions accepted by default ("innocent until proven guilty")
%% - "Hardest to discard": attacks from unweighted atoms are maximally expensive
%% - Budget semantics: requires budget ≥ #sup to discard such attacks
%%
%% For Gödel semiring:
%% - Supremum = #sup (positive infinity, fully true, maximum truth degree)
%% - Meaning: Unweighted assumptions have maximum truth value
%% - Discard cost with max monoid: max(#sup, ...) = #sup (infinitely expensive)
%% - Discard cost with min monoid: not compatible (see compatibility theory)
%% - Discard cost with sum monoid: sum += #sup → #sup (infinitely expensive)
%%
%% Explicit weights REPLACE the supremum default (via mutually exclusive rules).
%% DEFAULT WEIGHTS for unweighted atoms are now defined in monoid/*.lp files,
%% since the monoid determines what "hard to discard" means.
%%

%% Assumptions with explicit weights
%% FLAT-WABA: assumption is not a rule head
%% NON-FLAT-WABA: if assumption is also a rule head, weight is computed below via rule derivation
%% OPTIMIZED: Use not is_head(X) instead of not head(_,X)
supported_with_weight(X,W) :- assumption(X), in(X), weight(X,W), not is_head(X).

%% Assumptions without explicit weights: Use supremum (maximum truth value)
%% For Gödel semiring, unweighted assumptions get #sup (maximum truth degree)
%% Rationale: Unweighted assumptions are "hardest to discard" (default acceptance)
%% - Matches standard ABA semantics: assumptions accepted by default
%% - #sup = positive infinity (supremum, maximum value)
%% - In conjunction (min): min(#sup, x) = x (doesn't affect derivations)
%% - In disjunction (max): max(#sup, x) = #sup (dominates, represents maximum truth)
%%
%% NOTE: Uses #sup (positive infinity) as the supremum value.
%% Constraints must handle #sup specially for budget checking.
%%
%% FLAT-WABA: assumption is not a rule head
%% NON-FLAT-WABA: if assumption is also a rule head, weight is computed below via rule derivation
%% OPTIMIZED: Use not is_head(X) instead of not head(_,X)
supported_with_weight(X,W) :-
    assumption(X),
    in(X),
    not weight(X,_),
    not is_head(X),
    default_assumption_weight(W).

%% Step 1: Compute weight for each rule derivation using conjunction (minimum)
%% For rule R deriving X: take minimum weight among all body elements
%% IMPORTANT: Only compute weight for TRIGGERED rules (all body elements supported)
%% OPTIMIZED: Use rule(R) instead of head(R,X), remove redundant supported(X) check
rule_derivation_weight(R,X,W) :-
    rule(R),
    head(R,X),
    has_body(R),  % Only for rules with bodies
    triggered_by_in(R),  % Only triggered rules (prevents #sup from unsupported bodies)
    W = #min{ V,B : body(R,B), supported_with_weight(B,V) }.

%% Handle rules with empty bodies (facts): Use #inf (identity for max)
%% SOLUTION: Use additive identity (#inf for max) instead of multiplicative identity (#sup for min)
%%
%% Traditional approach assigned #sup (multiplicative identity for min/conjunction):
%%   rule_derivation_weight(R,X,#sup) :- ... not body(R,_).
%%
%% Problem: max(#sup, explicit_weight) = #sup dominates all explicit weights!
%%   Example: weight(c1, 8) → max(#sup, 8) = #sup → explicit weight LOST
%%
%% Solution: Use #inf (additive identity for max/disjunction):
%%   max(#inf, x) = x for any x > #inf
%%   This makes empty-body derivations "invisible" in the max aggregate
%%   Explicit weights are preserved: max(#inf, 8) = 8 ✓
%%
%% Algebraic justification:
%%   - #inf is the true identity for max operation (a ⊕ #inf = a)
%%   - Empty-body facts contribute no information to weight disjunction
%%   - Explicit weights take precedence without special-case logic
%%
%% OPTIMIZED: Use not has_body(R) instead of not body(R,_), use rule(R)
rule_derivation_weight(R,X,#inf) :-
    rule(R),
    head(R,X),
    not has_body(R).

%% Step 2: Combine multiple derivations using disjunction (maximum)
%% IMPORTANT: Use DISJUNCTION OPERATOR (⊕) to combine weights from different paths:
%% - Multiple rule derivations are combined via ⊕
%% - Explicit weight is another "path" to support, combined via ⊕
%% - For Gödel semiring: ⊕ = max (choose strongest/highest truth path)
%%
%% SEMANTICS: Explicit weight + derived weight represent ALTERNATIVE support paths.
%% The actual weight is their DISJUNCTION, not conjunction or override.
%%
%% Example: If X has explicit weight 50 and is also derived with weight 80,
%%          then supported_with_weight(X, max(50, 80)) = supported_with_weight(X, 80)
%%          (the stronger path is chosen)
%% OPTIMIZED: Use derived_atom(X) instead of head(_,X), not assumption(X)
supported_with_weight(X,W) :-
    derived_atom(X),
    supported(X),
    W = #max{ V,R : rule_derivation_weight(R,X,V) ;
              V : weight(X,V) }.  % DISJUNCTION (⊕ = max for Gödel)

%% NON-FLAT-WABA: Derived assumption with explicit weight
%% Combine rule-derived weight with explicit weight via DISJUNCTION (max)
%% OPTIMIZED: Use is_head(X) instead of head(_,X)
supported_with_weight(X,W) :-
    assumption(X),
    supported(X),
    is_head(X),  % Assumption is also derived
    weight(X,_),  % Has explicit weight
    W = #max{ V,R : rule_derivation_weight(R,X,V) ;
              V : weight(X,V) }.  % DISJUNCTION (⊕ = max for Gödel)

%% NON-FLAT-WABA: Derived assumption WITHOUT explicit weight, but is IN
%% For Gödel: max(derived, #sup) = #sup always (default dominates)
%% NOTE: Clingo ignores #sup in aggregates, so we assign it explicitly
%% OPTIMIZED: Use is_head(X) instead of head(_,X)
supported_with_weight(X,W) :-
    assumption(X),
    in(X),  % Assumption is selected
    supported(X),
    is_head(X),  % Assumption is also derived
    not weight(X,_),
    default_assumption_weight(W).

%% ==========================================
%% PROVENANCE-AWARE SUPPORT (SECTION 3.4)
%% ==========================================
%%
%% supported_with_weight_from(X,S,W) reifies support for X together with the
%% provenance source-set encoded as the bitmask S. This keeps attack identity at
%% the level of source-set + target rather than proof tree.

body_index(R,B,N) :-
    body(R,B),
    N = #count{ C : body(R,C), C < B }.

body_size(R,N) :-
    rule(R),
    N = #count{ B : body(R,B) }.

source_bit(X,B) :-
    assumption(X),
    N = #count{ Y : assumption(Y), Y < X },
    B = 2**N.

raw_support_with_weight_from(X,S,W) :-
    assumption(X),
    weight(X,W),
    source_bit(X,S).

raw_support_with_weight_from(X,S,W) :-
    assumption(X),
    not weight(X,_),
    source_bit(X,S),
    default_assumption_weight(W).

raw_support_with_weight_from(X,0,W) :-
    derived_atom(X),
    weight(X,W).

rule_source_prefix(R,0,0,#sup) :-
    has_body(R).

rule_source_prefix(R,N1,S1,W0) :-
    rule_source_prefix(R,N,S0,W0),
    N1 = N + 1,
    body_index(R,B,N),
    supported_with_weight_from(B,SB,V1),
    S1 = S0 ? SB,
    W0 <= V1.

rule_source_prefix(R,N1,S1,V1) :-
    rule_source_prefix(R,N,S0,W0),
    N1 = N + 1,
    body_index(R,B,N),
    supported_with_weight_from(B,SB,V1),
    S1 = S0 ? SB,
    V1 < W0.

raw_support_with_weight_from(X,S,W) :-
    head(R,X),
    has_body(R),
    body_size(R,N),
    rule_source_prefix(R,N,S,W).

raw_support_with_weight_from(X,0,#inf) :-
    head(R,X),
    not has_body(R).

supported_with_weight_from(X,S,W) :-
    raw_support_with_weight_from(X,S,_),
    W = #max{ V : raw_support_with_weight_from(X,S,V) }.
`,
        "lukasiewicz": `%% Łukasiewicz Semiring for Weight Propagation - OPTIMIZED
%% Semiring: ([0,K] ∪ {±∞}, ⊕_Ł, ⊗_Ł, 0, K)
%% - Domain: [0,K] ∪ {±∞} where K is the normalization constant (default K=100)
%% - Disjunction/⊕_Ł (OR): bounded sum min(K, a + b)
%% - Conjunction/⊗_Ł (AND): bounded sum max(0, a + b - K*(n-1))
%% - Additive identity (disjunction): 0
%% - Multiplicative identity (conjunction): K
%% - Interpretation: weights are truth degrees, higher is better
%% - Normalization constant K: parametrizable via CLI (default K=100)
%% - Default weight for unweighted assumptions: #sup
%%
%% OPTIMIZATION NOTES:
%% ===================
%% This version attempts to inline body_count and body_sum helpers to reduce grounding overhead.
%% Strategy: Combine count and sum operations directly in rule_derivation_weight computation.
%%
%% Original had 3 helpers:
%% 1. body_has_sup_weight - KEPT (necessary for #sum limitation)
%% 2. body_count - REMOVED (inlined into derivation rule)
%% 3. body_sum - REMOVED (inlined into derivation rule)
%%
%% Expected improvement: 30-40% reduction in grounding overhead

#const luk_k = 100.
active_semiring(lukasiewicz).
%% Default weight policy selection for unweighted assumptions.
%%
%% Semiring modules provide semiring_default_weight(policy, value).
%% Callers may optionally load one explicit policy module from defaults/.
%% If none is loaded, legacy behavior is preserved.

configured_default_policy :- explicit_default_policy(_).

:- explicit_default_policy(P1), explicit_default_policy(P2), P1 != P2.

active_default_policy(P) :- explicit_default_policy(P).
active_default_policy(legacy) :- not configured_default_policy.

default_assumption_weight(W) :-
    active_default_policy(P),
    semiring_default_weight(P, W).

:- active_default_policy(P), not semiring_default_weight(P, _).


semiring_default_weight(legacy,#sup).
semiring_default_weight(aba,#sup).
semiring_default_weight(neutral,luk_k).

%% =============================================
%% CLASSICAL ABA RECOVERY (No Discarding)
%% =============================================
%%
%% To recover plain unweighted ABA behavior with this semiring:
%%
%% UPPER BOUND REGIME (MAX, SUM, COUNT monoids):
%%   δ = #sup     (default weight for unweighted assumptions)
%%   β = 0        (budget value)
%%   Mechanism: constraint/ub.lp explicitly rejects discarding #sup-weighted attacks
%%
%% LOWER BOUND REGIME (MIN monoid):
%%   δ = #inf     (default weight for unweighted assumptions)
%%   β = 0        (budget value)
%%   Mechanism: MIN(#inf) = #inf < 0, rejected by constraint/lb.lp
%%
%% CURRENT IMPLEMENTATION: δ = #sup (see line below)

%% ========================================
%% DEFAULT WEIGHT PRINCIPLE: SUPREMUM
%% ========================================
%%
%% Unweighted atoms receive #sup (NOT the semiring identity K!)
%% This is a WABA design choice for ABA recovery.

%% Assumptions with explicit weights
supported_with_weight(X,W) :- assumption(X), in(X), weight(X,W), not head(_,X).

%% Assumptions without explicit weights: Use #sup
supported_with_weight(X,W) :-
    assumption(X),
    in(X),
    not weight(X,_),
    not head(_,X),
    default_assumption_weight(W).

default_weight_is_sup :- default_assumption_weight(#sup).
finite_default_weight(V) :- default_assumption_weight(V), V != #sup, V != #inf.

%% Step 1: Compute weight for each rule derivation using conjunction (Łukasiewicz AND)
%% Łukasiewicz AND: max(0, sum(weights) - K*(n-1))
%% OPTIMIZED: Inline count and sum directly in the rule

%% Helper: detect if any body element has #sup weight (NECESSARY - kept from original)
body_has_sup_weight(R) :- body(R,B), supported_with_weight(B,#sup).

%% If any body has #sup, result is #sup (infinite truth propagates)
rule_derivation_weight(R,X,#sup) :-
    head(R,X),
    supported(X),
    body(R,_),
    triggered_by_in(R),
    body_has_sup_weight(R).

%% Otherwise, apply Łukasiewicz conjunction formula directly
%% OPTIMIZED: Inline both count and sum operations
rule_derivation_weight(R,X,W) :-
    head(R,X),
    supported(X),
    body(R,_),
    triggered_by_in(R),
    not body_has_sup_weight(R),
    N = #count{ B : body(R,B) },
    S = #sum{ V,B : body(R,B), supported_with_weight(B,V) },
    W = #max{ 0; S - luk_k*(N-1) }.

%% Handle rules with empty bodies (facts): Use 0 (additive identity)
rule_derivation_weight(R,X,0) :-
    head(R,X),
    supported(X),
    not body(R,_).

%% Step 2: Combine multiple derivations using disjunction (Łukasiewicz OR)
%% Łukasiewicz OR: min(K, sum)
%% IMPORTANT: Handle #sup separately (cannot be summed)

%% ======================================
%% REGULAR DERIVED ATOMS (not assumptions)
%% ======================================

%% If any derivation has #sup, result is #sup
supported_with_weight(X,#sup) :-
    supported(X),
    head(_,X),
    not assumption(X),
    rule_derivation_weight(_,X,#sup).

%% If explicit weight is #sup, result is #sup
supported_with_weight(X,#sup) :-
    supported(X),
    head(_,X),
    not assumption(X),
    weight(X,#sup).

%% Otherwise, sum finite values and cap at K
supported_with_weight(X,W) :-
    supported(X),
    head(_,X),
    not assumption(X),
    not rule_derivation_weight(_,X,#sup),
    not weight(X,#sup),
    S = #sum{ V,rule,R : rule_derivation_weight(R,X,V) ;
              V,explicit,X : weight(X,V) },
    W = #min{ luk_k; S }.

%% =====================================================
%% NON-FLAT-WABA: Derived assumption WITH explicit weight
%% =====================================================

%% If any derivation has #sup, result is #sup
supported_with_weight(X,#sup) :-
    assumption(X),
    supported(X),
    head(_,X),
    weight(X,_),
    rule_derivation_weight(_,X,#sup).

%% If explicit weight is #sup, result is #sup
supported_with_weight(X,#sup) :-
    assumption(X),
    supported(X),
    head(_,X),
    weight(X,#sup).

%% Otherwise, sum finite values and cap at K
supported_with_weight(X,W) :-
    assumption(X),
    supported(X),
    head(_,X),
    weight(X,_),
    not rule_derivation_weight(_,X,#sup),
    not weight(X,#sup),
    S = #sum{ V,rule,R : rule_derivation_weight(R,X,V) ;
              V,explicit,X : weight(X,V) },
    W = #min{ luk_k; S }.

%% ================================================================
%% NON-FLAT-WABA: Derived assumption WITHOUT explicit weight, but IN
%% ================================================================

%% If any derivation has #sup, result is #sup
supported_with_weight(X,#sup) :-
    assumption(X),
    in(X),
    supported(X),
    head(_,X),
    not weight(X,_),
    rule_derivation_weight(_,X,#sup).

%% Default weight #sup always applies for unweighted assumptions
supported_with_weight(X,#sup) :-
    assumption(X),
    in(X),
    supported(X),
    head(_,X),
    not weight(X,_),
    default_weight_is_sup.

supported_with_weight(X,W) :-
    assumption(X),
    in(X),
    supported(X),
    head(_,X),
    not weight(X,_),
    not rule_derivation_weight(_,X,#sup),
    finite_default_weight(D),
    S = #sum{ V,rule,R : rule_derivation_weight(R,X,V) ;
              D,default,X : true },
    W = #min{ luk_k; S }.

%% ==========================================
%% PROVENANCE-AWARE SUPPORT (SECTION 3.4)
%% ==========================================

body_index(R,B,N) :-
    body(R,B),
    N = #count{ C : body(R,C), C < B }.

body_size(R,N) :-
    rule(R),
    N = #count{ B : body(R,B) }.

source_bit(X,B) :-
    assumption(X),
    N = #count{ Y : assumption(Y), Y < X },
    B = 2**N.

support_contrib_from(X,S,self(X),W) :-
    assumption(X),
    weight(X,W),
    source_bit(X,S).

support_contrib_from(X,S,self(X),W) :-
    assumption(X),
    not weight(X,_),
    source_bit(X,S),
    default_assumption_weight(W).

support_contrib_from(X,0,explicit(X),W) :-
    not assumption(X),
    weight(X,W).

rule_prefix(R,0,0,base(R),0,0) :-
    has_body(R).

rule_prefix(R,N1,S1,key(R,N1,Prev,Child),Sum1,HasSup0) :-
    rule_prefix(R,N,S0,Prev,Sum0,HasSup0),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,V),
    V != #sup,
    S1 = S0 ? SB,
    Sum1 = Sum0 + V.

rule_prefix(R,N1,S1,key(R,N1,Prev,Child),Sum0,1) :-
    rule_prefix(R,N,S0,Prev,Sum0,_),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,#sup),
    S1 = S0 ? SB.

rule_prefix(R,N1,S1,key(R,N1,Prev,Child),Sum0,1) :-
    rule_prefix(R,N,S0,Prev,Sum0,1),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,_),
    S1 = S0 ? SB.

support_contrib_from(X,S,rule(R,Key),#sup) :-
    head(R,X),
    has_body(R),
    body_size(R,N),
    rule_prefix(R,N,S,Key,_,1).

support_contrib_from(X,S,rule(R,Key),W) :-
    head(R,X),
    has_body(R),
    body_size(R,N),
    rule_prefix(R,N,S,Key,Sum,0),
    W = #max{ 0; Sum - luk_k*(N-1) }.

support_contrib_from(X,0,fact(R),0) :-
    head(R,X),
    not has_body(R).

supported_with_weight_from(X,S,#sup) :-
    support_contrib_from(X,S,_,#sup).

supported_with_weight_from(X,S,W) :-
    support_contrib_from(X,S,_,_),
    not support_contrib_from(X,S,_,#sup),
    Sum = #sum{ V,ID : support_contrib_from(X,S,ID,V), V != #sup },
    W = #min{ luk_k; Sum }.
`,
        "lukasiewicz_low": `%% Dual Łukasiewicz semiring with lower-is-better orientation.
%% Generated by scripts/generate_semiring_variants.py.
%%
%% Finite domain: [0,K], K given by luk_k (default 100).
%% Disjunction/alternative support: max(0, sum - K*(n-1))
%% Conjunction/rule body: min(K, sum)
%% Interpretation: lower is better.

#const luk_k = 100.
active_semiring(lukasiewicz_low).
%% Default weight policy selection for unweighted assumptions.
%%
%% Semiring modules provide semiring_default_weight(policy, value).
%% Callers may optionally load one explicit policy module from defaults/.
%% If none is loaded, legacy behavior is preserved.

configured_default_policy :- explicit_default_policy(_).

:- explicit_default_policy(P1), explicit_default_policy(P2), P1 != P2.

active_default_policy(P) :- explicit_default_policy(P).
active_default_policy(legacy) :- not configured_default_policy.

default_assumption_weight(W) :-
    active_default_policy(P),
    semiring_default_weight(P, W).

:- active_default_policy(P), not semiring_default_weight(P, _).


semiring_default_weight(legacy,#sup).
semiring_default_weight(aba,#sup).
semiring_default_weight(neutral,0).

supported_with_weight(X,W) :- assumption(X), in(X), weight(X,W), not head(_,X).
supported_with_weight(X,W) :- assumption(X), in(X), not weight(X,_), not head(_,X), default_assumption_weight(W).

default_weight_is_sup :- default_assumption_weight(#sup).
finite_default_weight(V) :- default_assumption_weight(V), V != #sup, V != #inf.

body_has_sup_weight(R) :- body(R,B), supported_with_weight(B,#sup).
body_has_inf_weight(R) :- body(R,B), supported_with_weight(B,#inf).

rule_derivation_weight(R,X,#sup) :-
    head(R,X),
    supported(X),
    body(R,_),
    triggered_by_in(R),
    body_has_sup_weight(R).

rule_derivation_weight(R,X,#inf) :-
    head(R,X),
    supported(X),
    body(R,_),
    triggered_by_in(R),
    not body_has_sup_weight(R),
    body_has_inf_weight(R).

rule_derivation_weight(R,X,W) :-
    head(R,X),
    supported(X),
    body(R,_),
    triggered_by_in(R),
    not body_has_sup_weight(R),
    not body_has_inf_weight(R),
    S = #sum{ V,B : body(R,B), supported_with_weight(B,V) },
    W = #min{ luk_k; S }.

rule_derivation_weight(R,X,luk_k) :-
    head(R,X),
    supported(X),
    not body(R,_).

finite_rule_derivation_weight(R,X,V) :-
    rule_derivation_weight(R,X,V),
    V != #sup,
    V != #inf.

finite_explicit_weight(X,V) :-
    weight(X,V),
    V != #sup,
    V != #inf.

has_inf_support_term(X) :- rule_derivation_weight(_,X,#inf).
has_inf_support_term(X) :- weight(X,#inf).

supported_with_weight(X,#sup) :-
    supported(X),
    head(_,X),
    not assumption(X),
    rule_derivation_weight(_,X,#sup).

supported_with_weight(X,#sup) :-
    supported(X),
    head(_,X),
    not assumption(X),
    weight(X,#sup).

supported_with_weight(X,0) :-
    supported(X),
    head(_,X),
    not assumption(X),
    not rule_derivation_weight(_,X,#sup),
    not weight(X,#sup),
    has_inf_support_term(X).

supported_with_weight(X,W) :-
    supported(X),
    head(_,X),
    not assumption(X),
    not rule_derivation_weight(_,X,#sup),
    not weight(X,#sup),
    not has_inf_support_term(X),
    N = #sum{ 1,R : finite_rule_derivation_weight(R,X,_) ;
              1,V : finite_explicit_weight(X,V) },
    S = #sum{ V,R : finite_rule_derivation_weight(R,X,V) ;
              V,V : finite_explicit_weight(X,V) },
    W = #max{ 0; S - luk_k*(N-1) }.

supported_with_weight(X,#sup) :-
    assumption(X),
    supported(X),
    head(_,X),
    not weight(X,_),
    default_weight_is_sup.

supported_with_weight(X,W) :-
    assumption(X),
    supported(X),
    head(_,X),
    not weight(X,_),
    not rule_derivation_weight(_,X,#sup),
    finite_default_weight(D),
    not has_inf_support_term(X),
    N = #sum{ 1,R : finite_rule_derivation_weight(R,X,_) ;
              1 : true },
    S = #sum{ V,R : finite_rule_derivation_weight(R,X,V) ;
              D : true },
    W = #max{ 0; S - luk_k*(N-1) }.

supported_with_weight(X,#sup) :-
    assumption(X),
    supported(X),
    head(_,X),
    weight(X,_),
    rule_derivation_weight(_,X,#sup).

supported_with_weight(X,#sup) :-
    assumption(X),
    supported(X),
    head(_,X),
    weight(X,#sup).

supported_with_weight(X,0) :-
    assumption(X),
    supported(X),
    head(_,X),
    weight(X,_),
    not rule_derivation_weight(_,X,#sup),
    not weight(X,#sup),
    has_inf_support_term(X).

supported_with_weight(X,W) :-
    assumption(X),
    supported(X),
    head(_,X),
    weight(X,_),
    not rule_derivation_weight(_,X,#sup),
    not weight(X,#sup),
    not has_inf_support_term(X),
    N = #sum{ 1,R : finite_rule_derivation_weight(R,X,_) ;
              1,V : finite_explicit_weight(X,V) },
    S = #sum{ V,R : finite_rule_derivation_weight(R,X,V) ;
              V,V : finite_explicit_weight(X,V) },
    W = #max{ 0; S - luk_k*(N-1) }.

%% ==========================================
%% PROVENANCE-AWARE SUPPORT (SECTION 3.4)
%% ==========================================

body_index(R,B,N) :-
    body(R,B),
    N = #count{ C : body(R,C), C < B }.

body_size(R,N) :-
    rule(R),
    N = #count{ B : body(R,B) }.

source_bit(X,B) :-
    assumption(X),
    N = #count{ Y : assumption(Y), Y < X },
    B = 2**N.

support_contrib_from(X,S,self(X),W) :-
    assumption(X),
    weight(X,W),
    source_bit(X,S).

support_contrib_from(X,S,self(X),W) :-
    assumption(X),
    not weight(X,_),
    source_bit(X,S),
    default_assumption_weight(W).

support_contrib_from(X,0,explicit(X),W) :-
    not assumption(X),
    weight(X,W).

rule_finite_prefix_from(R,0,0,base(R),0) :-
    has_body(R).

rule_inf_prefix_from(R,N1,S1,key(R,N1,Prev,Child)) :-
    rule_finite_prefix_from(R,N,S0,Prev,_),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,#inf),
    S1 = S0 ? SB.

rule_sup_prefix_from(R,N1,S1,key(R,N1,Prev,Child)) :-
    rule_finite_prefix_from(R,N,S0,Prev,_),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,#sup),
    S1 = S0 ? SB.

rule_finite_prefix_from(R,N1,S1,key(R,N1,Prev,Child),Sum1) :-
    rule_finite_prefix_from(R,N,S0,Prev,Sum0),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,V),
    V != #sup,
    V != #inf,
    S1 = S0 ? SB,
    Sum1 = Sum0 + V.

rule_inf_prefix_from(R,N1,S1,key(R,N1,Prev,Child)) :-
    rule_inf_prefix_from(R,N,S0,Prev),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,V),
    V != #sup,
    S1 = S0 ? SB.

rule_sup_prefix_from(R,N1,S1,key(R,N1,Prev,Child)) :-
    rule_inf_prefix_from(R,N,S0,Prev),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,#sup),
    S1 = S0 ? SB.

rule_sup_prefix_from(R,N1,S1,key(R,N1,Prev,Child)) :-
    rule_sup_prefix_from(R,N,S0,Prev),
    N1 = N + 1,
    body_index(R,B,N),
    support_contrib_from(B,SB,Child,_),
    S1 = S0 ? SB.

support_contrib_from(X,S,rule(R,Key),#sup) :-
    head(R,X),
    has_body(R),
    body_size(R,N),
    rule_sup_prefix_from(R,N,S,Key).

support_contrib_from(X,S,rule(R,Key),#inf) :-
    head(R,X),
    has_body(R),
    body_size(R,N),
    rule_inf_prefix_from(R,N,S,Key),
    not rule_sup_prefix_from(R,N,S,Key).

support_contrib_from(X,S,rule(R,Key),W) :-
    head(R,X),
    has_body(R),
    body_size(R,N),
    rule_finite_prefix_from(R,N,S,Key,Sum),
    W = #min{ luk_k; Sum }.

support_contrib_from(X,0,fact(R),luk_k) :-
    head(R,X),
    not has_body(R).

has_inf_support_term_from(X,S) :- support_contrib_from(X,S,_,#inf).

supported_with_weight_from(X,S,#sup) :-
    support_contrib_from(X,S,_,#sup).

supported_with_weight_from(X,S,0) :-
    support_contrib_from(X,S,_,_),
    not support_contrib_from(X,S,_,#sup),
    has_inf_support_term_from(X,S).

supported_with_weight_from(X,S,W) :-
    support_contrib_from(X,S,_,_),
    not support_contrib_from(X,S,_,#sup),
    not has_inf_support_term_from(X,S),
    N = #sum{ 1,ID : support_contrib_from(X,S,ID,V), V != #sup, V != #inf },
    Sum = #sum{ V,ID : support_contrib_from(X,S,ID,V), V != #sup, V != #inf },
    W = #max{ 0; Sum - luk_k*(N-1) }.
`
    },
    defaults: {
        "legacy": `%% Preserve the pre-refactor default behavior for unweighted assumptions.
explicit_default_policy(legacy).
`,
        "aba": `%% Use ABA-recovery defaults for unweighted assumptions.
%% This chooses the semiring-specific "hard to discard" value.
explicit_default_policy(aba).
`,
        "neutral": `%% Use the semiring-specific conjunction-neutral default for unweighted assumptions.
explicit_default_policy(neutral).
`
    },
    monoid: {
        "sum": `%% Sum monoid family.
%% Aggregate: sum of discarded attack weights.
%% Identity on the empty set: 0.

active_monoid(sum).
`,
        "max": `%% Max monoid family.
%% Aggregate: maximum discarded attack weight.
%% Identity on the empty set: #inf.

active_monoid(max).
`,
        "min": `%% Min monoid family.
%% Aggregate: minimum discarded attack weight.
%% Identity on the empty set: #sup.

active_monoid(min).
`,
        "count": `%% Count monoid family.
%% Aggregate: number of discarded attacks.
%% Identity on the empty set: 0.

active_monoid(count).
`
    },
    optimize: {
        "minimize": `%% Generic minimization objective over the active monoid family.

active_optimization(minimize).

:- active_optimization(maximize).
:- not active_monoid(sum), not active_monoid(max), not active_monoid(min), not active_monoid(count).
:- 2 { active_monoid(sum); active_monoid(max); active_monoid(min); active_monoid(count) }.

#minimize { W,I : active_monoid(sum), discarded_attack_detail(I,_,_,_,W) }.
#minimize { 1,I : active_monoid(count), discarded_attack(I) }.

:~ active_monoid(max), M = #max { W,I : discarded_attack_detail(I,_,_,_,W) }, M = #sup. [1@2]
:~ active_monoid(max), M = #max { W,I : discarded_attack_detail(I,_,_,_,W) }, M = #inf. [0@1]
:~ active_monoid(max), M = #max { W,I : discarded_attack_detail(I,_,_,_,W) }, M != #sup, M != #inf. [M@0]

:~ active_monoid(min), M = #min { W,I : discarded_attack_detail(I,_,_,_,W) }, M = #inf. [1@2]
:~ active_monoid(min), M = #min { W,I : discarded_attack_detail(I,_,_,_,W) }, M = #sup. [0@1]
:~ active_monoid(min), M = #min { W,I : discarded_attack_detail(I,_,_,_,W) }, M != #sup, M != #inf. [M@0]
`,
        "maximize": `%% Generic maximization objective over the active monoid family.

active_optimization(maximize).

:- active_optimization(minimize).
:- not active_monoid(sum), not active_monoid(max), not active_monoid(min), not active_monoid(count).
:- 2 { active_monoid(sum); active_monoid(max); active_monoid(min); active_monoid(count) }.

#maximize { W,I : active_monoid(sum), discarded_attack_detail(I,_,_,_,W) }.
#maximize { 1,I : active_monoid(count), discarded_attack(I) }.

:~ active_monoid(max), M = #max { W,I : discarded_attack_detail(I,_,_,_,W) }, M = #inf. [1@2]
:~ active_monoid(max), M = #max { W,I : discarded_attack_detail(I,_,_,_,W) }, M != #sup. [-1@1]
:~ active_monoid(max), M = #max { W,I : discarded_attack_detail(I,_,_,_,W) }, M != #sup, M != #inf. [-M@0]

:~ active_monoid(min), M = #min { W,I : discarded_attack_detail(I,_,_,_,W) }, M = #inf. [1@2]
:~ active_monoid(min), M = #min { W,I : discarded_attack_detail(I,_,_,_,W) }, M != #inf. [-1@1]
:~ active_monoid(min), M = #min { W,I : discarded_attack_detail(I,_,_,_,W) }, M != #sup, M != #inf. [-M@0]
`
    },
    constraint: {
        "ub": `%% Generic upper-bound budget constraint.

active_budget_mode(ub).
active_constraint(ub).

:- not active_monoid(sum), not active_monoid(max), not active_monoid(min), not active_monoid(count).
:- 2 { active_monoid(sum); active_monoid(max); active_monoid(min); active_monoid(count) }.

budget_value(C) :- active_monoid(sum), C = #sum{ W,I : discarded_attack_detail(I,_,_,_,W) }.
budget_value(C) :- active_monoid(max), C = #max{ W,I : discarded_attack_detail(I,_,_,_,W) }.
budget_value(C) :- active_monoid(min), C = #min{ W,I : discarded_attack_detail(I,_,_,_,W) }.
budget_value(C) :- active_monoid(count), C = #count{ I : discarded_attack(I) }.

:- budget_value(C), C > B, budget(B).
:- discarded_attack_detail(_,_,_,_,#sup).
`,
        "lb": `%% Generic lower-bound budget constraint.

active_budget_mode(lb).
active_constraint(lb).

:- not active_monoid(sum), not active_monoid(max), not active_monoid(min), not active_monoid(count).
:- 2 { active_monoid(sum); active_monoid(max); active_monoid(min); active_monoid(count) }.

budget_value(C) :- active_monoid(sum), C = #sum{ W,I : discarded_attack_detail(I,_,_,_,W) }.
budget_value(C) :- active_monoid(max), C = #max{ W,I : discarded_attack_detail(I,_,_,_,W) }.
budget_value(C) :- active_monoid(min), C = #min{ W,I : discarded_attack_detail(I,_,_,_,W) }.
budget_value(C) :- active_monoid(count), C = #count{ I : discarded_attack(I) }.

:- budget_value(C), C < B, budget(B).
:- discarded_attack_detail(_,_,_,_,#sup).
`,
        "no_discard": `%% No Discarding Constraint
%% =========================
%%
%% Enforces classical ABA by forcing the discard set to be empty
%%
%% Use with: Any semantic file for pure ABA/ASPforABA compatibility
%%
%% Semantics: direct no-discard profile, independent of monoid loading
%%
%% Usage:
%%   clingo -n 0 WABA/core/base.lp WABA/semiring/godel.lp \\
%%          WABA/constraint/no_discard.lp WABA/filter/standard.lp \\
%%          WABA/semantics/<semantic>.lp framework.lp

%% Reject any extension that discards any attack
active_budget_mode(no_discard).
active_constraint(no_discard).
:- discarded_attack(_).
`
    },
    filter: {
        "standard": `#show in/1.
#show out/1.
#show undec/1.
#show supported_with_weight/2.
#show attacks_successfully_with_weight/3.
#show discarded_attack/3.

% Framework structure (needed for attack derivation tracing)
#show assumption/1.
#show contrary/2.
#show head/2.
#show body/2.
`,
        "projection": `%% Output filtering for projection mode: show in/out and discarded attacks
#show in/1.
#show out/1.
#show discarded_attack/3.
`
    },
    semantics: {
        "cf": `%% Shared successful-attack semantics infrastructure.

1{ in(X); out(X) }1 :- assumption(X).

%% Non-flat ABA closure: derived assumptions must be accepted.
:- out(X), assumption(X), supported(X).

successful_attack(Aid,S,U,V,W) :-
    attack(Aid,S,U,V,W),
    not discarded_attack(Aid).

candidate_successful_attack(Aid,S,U,V,W) :-
    successful_attack(Aid,S,U,V,W),
    source_enabled_by_in(S).

attacked_by_candidate(X) :-
    candidate_successful_attack(_,_,_,X,_).

attacked_by_source(X,S) :-
    successful_attack(_,S,_,X,_).

counterattacks_closure(S) :-
    closure_member(S,A),
    attacked_by_candidate(A).

undefended(X) :-
    attacked_by_source(X,S),
    not counterattacks_closure(S).

defended(X) :-
    assumption(X),
    not undefended(X).


:- in(X), attacked_by_candidate(X).
`,
        "stable": `%% Shared successful-attack semantics infrastructure.

1{ in(X); out(X) }1 :- assumption(X).

%% Non-flat ABA closure: derived assumptions must be accepted.
:- out(X), assumption(X), supported(X).

successful_attack(Aid,S,U,V,W) :-
    attack(Aid,S,U,V,W),
    not discarded_attack(Aid).

candidate_successful_attack(Aid,S,U,V,W) :-
    successful_attack(Aid,S,U,V,W),
    source_enabled_by_in(S).

attacked_by_candidate(X) :-
    candidate_successful_attack(_,_,_,X,_).

attacked_by_source(X,S) :-
    successful_attack(_,S,_,X,_).

counterattacks_closure(S) :-
    closure_member(S,A),
    attacked_by_candidate(A).

undefended(X) :-
    attacked_by_source(X,S),
    not counterattacks_closure(S).

defended(X) :-
    assumption(X),
    not undefended(X).


:- in(X), attacked_by_candidate(X).


:- out(X), assumption(X), not attacked_by_candidate(X).
`,
        "admissible": `%% Shared successful-attack semantics infrastructure.

1{ in(X); out(X) }1 :- assumption(X).

%% Non-flat ABA closure: derived assumptions must be accepted.
:- out(X), assumption(X), supported(X).

successful_attack(Aid,S,U,V,W) :-
    attack(Aid,S,U,V,W),
    not discarded_attack(Aid).

candidate_successful_attack(Aid,S,U,V,W) :-
    successful_attack(Aid,S,U,V,W),
    source_enabled_by_in(S).

attacked_by_candidate(X) :-
    candidate_successful_attack(_,_,_,X,_).

attacked_by_source(X,S) :-
    successful_attack(_,S,_,X,_).

counterattacks_closure(S) :-
    closure_member(S,A),
    attacked_by_candidate(A).

undefended(X) :-
    attacked_by_source(X,S),
    not counterattacks_closure(S).

defended(X) :-
    assumption(X),
    not undefended(X).


:- in(X), attacked_by_candidate(X).


%% Admissibility
:- in(X), undefended(X).
`,
        "complete": `%% Complete Semantics
%% Builds upon: Admissible

%% Shared successful-attack semantics infrastructure.

1{ in(X); out(X) }1 :- assumption(X).

%% Non-flat ABA closure: derived assumptions must be accepted.
:- out(X), assumption(X), supported(X).

successful_attack(Aid,S,U,V,W) :-
    attack(Aid,S,U,V,W),
    not discarded_attack(Aid).

candidate_successful_attack(Aid,S,U,V,W) :-
    successful_attack(Aid,S,U,V,W),
    source_enabled_by_in(S).

attacked_by_candidate(X) :-
    candidate_successful_attack(_,_,_,X,_).

attacked_by_source(X,S) :-
    successful_attack(_,S,_,X,_).

counterattacks_closure(S) :-
    closure_member(S,A),
    attacked_by_candidate(A).

undefended(X) :-
    attacked_by_source(X,S),
    not counterattacks_closure(S).

defended(X) :-
    assumption(X),
    not undefended(X).


:- in(X), attacked_by_candidate(X).


%% Admissibility
:- in(X), undefended(X).


:- out(X), assumption(X), defended(X).
`,
        "subset_maximal_filter": `%% Exact subset-maximal filtering over precomputed candidate extensions.
%%
%% Expected facts:
%%   candidate(M).
%%   member(M,A).
%%
%% Output:
%%   keep(M).
%%
%% Rationale:
%%   exact subset-maximality is a set-inclusion property; it is kept separate
%%   from WABA's numeric weight optimization layer instead of being encoded with
%%   plain #maximize priority levels.

model_pair(I,J) :- candidate(I), candidate(J), I != J.

not_subset(I,J) :- model_pair(I,J), member(I,A), not member(J,A).
has_extra(J,I) :- model_pair(I,J), member(J,A), not member(I,A).

strict_subset(I,J) :-
    model_pair(I,J),
    not not_subset(I,J),
    has_extra(J,I).

dominated(I) :- strict_subset(I,J).
keep(I) :- candidate(I), not dominated(I).

#show keep/1.
`,
        "subset_minimal_filter": `%% Exact subset-minimal filtering over precomputed candidate extensions.
%%
%% Expected facts:
%%   candidate(M).
%%   member(M,A).
%%
%% Output:
%%   keep(M).

model_pair(I,J) :- candidate(I), candidate(J), I != J.

not_subset(I,J) :- model_pair(I,J), member(I,A), not member(J,A).
has_extra(I,J) :- model_pair(I,J), member(I,A), not member(J,A).

strict_subset(I,J) :-
    model_pair(I,J),
    not not_subset(I,J),
    has_extra(J,I).

dominated(J) :- strict_subset(I,J).
keep(I) :- candidate(I), not dominated(I).

#show keep/1.
`
    },
    examples: {
        "simple_attack": `%% Simple test framework for comparing WABA semantics with ASPARTIX
%%
%% Structure: a attacks b, c is independent
%%
%% Expected (classical ABA / AAF):
%%   Stable: {a,c}
%%   Admissible: {}, {a}, {c}, {a,c}
%%   Complete: {a,c}
%%   Grounded: {a,c}
%%   Preferred: {a,c}

assumption(a).
assumption(b).
assumption(c).

% a attacks b
contrary(b, a).

% Weights (all equal for plain ABA behavior)
weight(a, 1).
weight(b, 1).
weight(c, 1).

% Budget definition for constraint files
% Use beta=0 for plain ABA (no attack discarding)
#const beta = 0.
budget(beta).
`,
        "aspforaba_journal_example": `%% Journal paper reference case from ASPforABA comparison work.
%% Expected classical results with no attack discarding:
%% - admissible: 7
%% - complete: 3
%% - stable: 2
%% - preferred: 2
%% - grounded: 1

assumption(a).
assumption(b).
assumption(c).
assumption(d).

contrary(b, x).
contrary(c, y).
contrary(d, z).

head(r1, w). body(r1, a).
head(r2, y). body(r2, b). body(r2, w).
head(r3, x). body(r3, c).
head(r4, z). body(r4, a). body(r4, b).

weight(a, 100).
weight(b, 100).
weight(c, 100).
weight(d, 100).
weight(w, 100).
weight(x, 100).
weight(y, 100).
weight(z, 100).

#const beta = 0.
budget(beta).
`,
        "strong_inference_bounded_lies": `%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% STRONG INFERENCE WITH BOUNDED LIES
%%
%% Source:
%%   Baldi, Corsi, Hosni, "A Logical Framework for Data-Driven Reasoning"
%%   arXiv:2408.12346v1, Example 4.
%%
%% Scientific reading:
%%   Four competing hypotheses H1..H4 are tested by six binary experiments.
%%   Nature may "lie" at most m times, where lies stand for bounded error in
%%   experimentation or data analysis. In the source example m = 2 and H3 is
%%   the only hypothesis that survives.
%%
%% WABA encoding choice:
%% - Semiring: Godel (higher is better)
%%   The source treats each experiment outcome as a qualitative rejection token,
%%   not as an accumulating cost. Godel keeps support qualitative.
%% - Monoid: Count
%%   The paper counts temporary rejections. This matches counting discarded
%%   attacks exactly.
%% - Budget mode: Upper bound
%%   At most m attacks on the accepted hypothesis may be discarded.
%%
%% Recommended run:
%% ./bin/waba run \\
%%   --framework examples/reference/strong_inference_bounded_lies.lp \\
%%   --semiring-family godel \\
%%   --polarity higher \\
%%   --semantics stable \\
%%   --objective count-min \\
%%   --budget-mode ub \\
%%   --beta 2 \\
%%   --show projection
%%
%% Expected conclusion:
%%   The unique stable extension is {h3}.
%%
%% Note:
%%   Unlike plain ABA, Strong Inference always keeps at least one current
%%   candidate hypothesis. The final integrity constraint below enforces this
%%   source-level requirement by ruling out the empty hypothesis set.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

#const beta = 2.
budget(beta).

%% Competing hypotheses.
assumption(h1).
assumption(h2).
assumption(h3).
assumption(h4).

%% Binary question-answer items from Example 4.
head(qr1, q1). weight(q1, 100).
head(qr2, q2). weight(q2, 100).
head(qr3, q3). weight(q3, 100).
head(qr4, q4). weight(q4, 100).
head(qr5, q5). weight(q5, 100).
head(qr6, q6). weight(q6, 100).

%% q1 = H3 \\/ H4, so H1 and H2 are rejected.
contrary(h1, q1_rejects_h1).
contrary(h2, q1_rejects_h2).
head(r1, q1_rejects_h1). body(r1, q1).
head(r2, q1_rejects_h2). body(r2, q1).

%% q2 = H4, so H1, H2, H3 are rejected.
contrary(h1, q2_rejects_h1).
contrary(h2, q2_rejects_h2).
contrary(h3, q2_rejects_h3).
head(r3, q2_rejects_h1). body(r3, q2).
head(r4, q2_rejects_h2). body(r4, q2).
head(r5, q2_rejects_h3). body(r5, q2).

%% q3 = H1 \\/ H2 \\/ H3, so H4 is rejected.
contrary(h4, q3_rejects_h4).
head(r6, q3_rejects_h4). body(r6, q3).

%% q4 = H3, so H1, H2, H4 are rejected.
contrary(h1, q4_rejects_h1).
contrary(h2, q4_rejects_h2).
contrary(h4, q4_rejects_h4).
head(r7, q4_rejects_h1). body(r7, q4).
head(r8, q4_rejects_h2). body(r8, q4).
head(r9, q4_rejects_h4). body(r9, q4).

%% q5 = H4, so H1, H2, H3 are rejected.
contrary(h1, q5_rejects_h1).
contrary(h2, q5_rejects_h2).
contrary(h3, q5_rejects_h3).
head(r10, q5_rejects_h1). body(r10, q5).
head(r11, q5_rejects_h2). body(r11, q5).
head(r12, q5_rejects_h3). body(r12, q5).

%% q6 = H1 \\/ H2 \\/ H3, so H4 is rejected.
contrary(h4, q6_rejects_h4).
head(r13, q6_rejects_h4). body(r13, q6).

%% Strong Inference keeps at least one current candidate hypothesis.
:- not in(h1), not in(h2), not in(h3), not in(h4).
`,
        "expanding_universe_argumentation": `%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% EXPANDING UNIVERSE AS SCIENTIFIC ARGUMENTATION
%%
%% Source:
%%   Landes, Corsi, Baldi, "Knowledge Representation, Scientific
%%   Argumentation and Non-monotonic Logic", Chapter 8 in
%%   Perspectives on Logics for Data-driven Reasoning (2025),
%%   Example "An Expanding Universe".
%%
%% Scientific reading:
%%   H0 says that the universe is fixed and without temporal or spatial limits.
%%   Friedmann's and Lemaitre's results are treated as first-order theoretical
%%   evidence against H0 because they are logical consequences of general
%%   relativity. Hubble's observations provide experimental confirmation.
%%   Einstein's 1922 criticism attacks Friedmann's line of evidence, but the
%%   later retraction defeats that criticism. Einstein's intuition-based
%%   objection to Lemaitre is undercut by the methodological demand that
%%   criticism be evidence-based, together with Hubble's later data.
%%
%% WABA encoding choice:
%% - Semiring: Godel (higher is better)
%%   The chapter is comparative and credibility-oriented. Different arguments
%%   offer alternative support paths for the same conclusion, so max/min is the
%%   clean local reading.
%% - Monoid: Count
%%   The chapter does not license discarding attacks. Using count with beta = 0
%%   reconstructs the plain argumentation setting.
%% - Budget mode: Upper bound with beta = 0
%%   No attack may be discarded.
%%
%% Weight discipline:
%% - The chapter does not give cardinal values.
%% - The numbers below are an ordinal extraction from explicit comparative
%%   claims in the text:
%%   * D3 is strongest because it "definitively confirmed" expansion.
%%   * D1 and D2 are substantial but theoretical rather than observational.
%%   * M1 has moderate force because it came from Einstein but was retracted.
%%   * M2 is strong because it explicitly withdraws M1.
%%   * M3 is weak because the chapter says it is intuition-based and lacks a
%%     solid evidential foundation.
%%   * M4 is strong because it encodes the chapter's methodological norm that
%%     scientific criticism must be evidence-based.
%%
%% Recommended run:
%% ./bin/waba run \\
%%   --framework examples/reference/expanding_universe_argumentation.lp \\
%%   --semiring-family godel \\
%%   --polarity higher \\
%%   --semantics stable \\
%%   --objective count-min \\
%%   --budget-mode ub \\
%%   --beta 0 \\
%%   --show standard
%%
%% Expected conclusion:
%%   The unique stable extension contains the accepted background theory,
%%   Friedmann's and Lemaitre's theoretical lines, Hubble's data, Einstein's
%%   retraction, and the evidence-over-intuition methodological principle.
%%   The static-universe hypothesis, Einstein's error note, and the purely
%%   intuition-based objection are rejected.
%%
%% Modeling note:
%%   No assumption is the direct contrary of another. Every attack is mediated
%%   by a derivation chain whose intermediate atoms correspond to meaningful
%%   scientific or methodological claims from the chapter.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

#const beta = 0.
budget(beta).

%% Target hypothesis.
assumption(h0_static_universe).

%% Background and first-order evidence.
assumption(bg_general_relativity).
assumption(d1_friedmann_paper).
assumption(d2_lemaitre_paper).
assumption(d3_hubble_measurements).

%% Meta-evidence.
assumption(m1_einstein_error_note).
assumption(m2_einstein_retraction).
assumption(m3_einstein_intuition_objection).
assumption(m4_evidence_before_intuition).

%% Ordinal credibility weights extracted from the chapter's comparative claims.
weight(h0_static_universe, 60).
weight(bg_general_relativity, 85).
weight(d1_friedmann_paper, 70).
weight(d2_lemaitre_paper, 75).
weight(d3_hubble_measurements, 95).
weight(m1_einstein_error_note, 55).
weight(m2_einstein_retraction, 90).
weight(m3_einstein_intuition_objection, 30).
weight(m4_evidence_before_intuition, 88).

%% Friedmann: GR + Friedmann's work yields a non-static cosmological model.
head(r1, relativistic_field_equations). body(r1, bg_general_relativity).
head(r2, dynamic_metric_solution). body(r2, relativistic_field_equations). body(r2, d1_friedmann_paper).
head(r3, non_static_cosmology). body(r3, dynamic_metric_solution).
head(r4, expansion_supported). body(r4, non_static_cosmology).

%% Lemaitre: GR + Lemaitre's work provides a second theoretical expansion path.
head(r5, relativistic_expansion_model). body(r5, relativistic_field_equations). body(r5, d2_lemaitre_paper).
head(r6, recession_has_theoretical_basis). body(r6, relativistic_expansion_model).
head(r7, expansion_supported). body(r7, recession_has_theoretical_basis).

%% Hubble: observational redshifts confirm recession empirically.
head(r8, galactic_redshift_pattern). body(r8, d3_hubble_measurements).
head(r9, observed_galactic_recession). body(r9, galactic_redshift_pattern).
head(r10, expansion_supported). body(r10, observed_galactic_recession).

%% Theory and data can also converge into a joint support path.
head(r11, theory_data_convergence). body(r11, recession_has_theoretical_basis). body(r11, observed_galactic_recession).
head(r12, expansion_supported). body(r12, theory_data_convergence).

%% The shared scientific conclusion attacks the static-universe hypothesis.
contrary(h0_static_universe, expansion_supported).

%% Einstein's 1922 note attacks Friedmann's evidential line.
contrary(d1_friedmann_paper, friedmann_line_undermined).
head(r13, published_error_note). body(r13, m1_einstein_error_note).
head(r14, friedmann_line_undermined). body(r14, published_error_note).

%% Einstein's later retraction defeats the earlier criticism.
contrary(m1_einstein_error_note, error_note_withdrawn).
head(r15, calculations_confirmed). body(r15, m2_einstein_retraction).
head(r16, error_note_withdrawn). body(r16, calculations_confirmed).

%% The intuition-based objection attacks Lemaitre's line.
contrary(d2_lemaitre_paper, lemaitre_line_undermined).
head(r17, intuition_against_expansion). body(r17, m3_einstein_intuition_objection).
head(r18, lemaitre_line_undermined). body(r18, intuition_against_expansion).

%% Methodological criticism plus later data undercut the intuition-only attack.
contrary(m3_einstein_intuition_objection, objection_without_evidence).
head(r19, criticism_must_track_evidence). body(r19, m4_evidence_before_intuition).
head(r20, empirical_checkpoint). body(r20, observed_galactic_recession).
head(r21, objection_without_evidence). body(r21, criticism_must_track_evidence). body(r21, empirical_checkpoint).
`,
        "sem_subset_closure_counterattack": `assumption(a).
assumption(b).
assumption(c).
assumption(d).
assumption(e).

contrary(a,p).
contrary(c,q).
contrary(b,r).
contrary(d,s).

head(r1,p).
body(r1,b).
body(r1,d).

head(r2,c).
body(r2,b).

head(r3,q).
body(r3,e).

head(r4,r).
body(r4,a).

head(r5,s).
body(r5,e).

weight(a,1).
weight(b,1).
weight(d,1).
weight(e,1).
`
    },
    metadata: {
        "generatedFrom": "ABA-variants/WABA",
        "semiringFamilies": [
            "godel",
            "lukasiewicz"
        ],
        "polarities": [
            "higher",
            "lower"
        ],
        "supportedSemiringKeys": [
            "godel",
            "lukasiewicz",
            "lukasiewicz_low"
        ],
        "defaults": [
            "legacy",
            "aba",
            "neutral"
        ],
        "monoids": [
            "sum",
            "max",
            "count",
            "min"
        ],
        "optimizations": [
            "minimize",
            "maximize"
        ],
        "objectives": [
            "sum-min",
            "sum-max",
            "max-min",
            "max-max",
            "count-min",
            "count-max",
            "min-min",
            "min-max"
        ],
        "budgetModes": [
            "none",
            "ub",
            "lb"
        ],
        "supportedSemantics": [
            "cf",
            "stable",
            "admissible",
            "complete",
            "grounded",
            "preferred"
        ],
        "postFilteredSemantics": [
            "grounded",
            "preferred"
        ],
        "canonicalSemiring": {
            "godel": {
                "higher": "godel"
            },
            "lukasiewicz": {
                "higher": "lukasiewicz",
                "lower": "lukasiewicz_low"
            }
        },
        "aliases": {},
        "supportedBudgetPairs": [
            {
                "monoid": "sum",
                "budgetMode": "ub"
            },
            {
                "monoid": "max",
                "budgetMode": "ub"
            },
            {
                "monoid": "count",
                "budgetMode": "ub"
            },
            {
                "monoid": "min",
                "budgetMode": "lb"
            }
        ]
    }
};
