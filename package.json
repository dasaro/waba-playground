{
  "name": "waba-playground",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-modules.js",
    "build": "npm run sync",
    "dev": "npm run sync && npx http-server -p 8080"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
